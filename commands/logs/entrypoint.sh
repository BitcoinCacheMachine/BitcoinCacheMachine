#!/bin/bash

set -Eeuo pipefail
cd "$(dirname "$0")"

# BCM_CLI_COMMAND=

# if [[ ! -z ${1+x} ]]; then
#     BCM_CLI_COMMAND="$1"
# else
#     cat ./help.txt
#     exit
# fi

# export BCM_CLI_COMMAND="$BCM_CLI_COMMAND"
# CHAIN="$BCM_ACTIVE_CHAIN"
# # CHAIN_TEXT="-$CHAIN"
