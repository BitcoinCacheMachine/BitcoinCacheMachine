Description:
  Signs a merge and verifies signatures on the tips of the branches.

Usage:
  bcm git merge --tag="v0.0.1" --message="Version 1"

Parameters:
  --branch-name        Specify the branch name that will be merged with the master.

Optional Parameters:
  --verify-signatures  Verify if the tips of the branches have been signed with trusted keys.
  
