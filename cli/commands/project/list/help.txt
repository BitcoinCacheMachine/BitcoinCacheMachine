Description:
  List existing BCM project definitions or project deployments.

Usage:
  bcm project list

Flags:
    --deployments       If present, list the cluster deployments for a BCM project definition.
                            --project-name must be set.
    
    --project-name=     Optional. Needed when using --deployments.
    <BCM_PROJECT_NAME>      
 
