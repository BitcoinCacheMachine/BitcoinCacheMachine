Description:
  Lists the BCM clusters under management.

Usage:
  bcm cluster list

Parameters:
  -c=<BCM_CLUSTER_NAME>   The name of the new cluster you want to focus on.
  -q | --endpoints        Flag. Optional. Returns the name of each member 
                          (i.e., LXD endpoint) in specified cluster (-c).
 
